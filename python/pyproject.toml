[project]
name = "riemann-framework"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mpmath>=1.3.0",
    "numpy>=1.26.0",
    "scipy>=1.11.0",
    "matplotlib>=3.8.0",
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]